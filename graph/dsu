
#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std;

vector<int> parent;
int fup(int x)//find ultimate parent w path compression
{
     if(parent[x]==x)
     return x;
     else
     {
        parent[x]=fup(parent[x]);
        return parent[x];
     }
}
string solve()
{
        int n;
        cin>>n;
        vector<vector<int>> vi(n,vector<int>(2,-1));
        bool f=0;
        for(int i=0;i<n;i++)
        {
            cin>>vi[i][0]>>vi[i][1];
        }
        parent.resize(n);
        for(int i=0;i<n;i++)
        {
           parent[i]=i;
        }
        vector<int> size(n,1);
        vector<int> last(n+1,-1);
                int i;int n1;
                int u=fup(i); //connecting u and v start
                int v=fup(last[n1]); 
                if(u!= v)
                {
                    if(size[v]>size[u])
                    {
                    parent[u]=v;
                    size[v]+=size[u];
                    }
                    else
                    {
                    parent[v]=u;
                    size[u]+=size[v];
                    }
                }//connecting u and v end
           
            for(int i=0;i<n;i++)
            {
                int x=fup(i);
                if(x==i)
                {
                   if(size[x]%2==1) //cycle of odd length
                   return "NO";
                }
            }
            return "YES";
 }
        

int main()
{
    int nn;
    cin>>nn;
    for(int ii=0;ii<nn;ii++)
    {
        cout<<solve()<<endl;
    }

}